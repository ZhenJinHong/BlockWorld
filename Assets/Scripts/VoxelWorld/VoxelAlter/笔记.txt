区块系统只针对范围变化后大区块的生成及卸载,以及生成后在主线程写入,

体素修改系统是无法放在物理系统执行之前,因为修改后必须立刻检查是否需要重构碰撞体,那么这个修改体素的行为最好在物理之后,修改后,有时间重构碰撞体

体素修改系统,修改体素,这个应该放在工作线程?
可以在区块系统之前先读取体素图计算需要修改体素,然后在修改体素系统组里写入体素图?

以下方式之一
1?:
这个写入是要按照主线程?
写入后以便网格构建系统立刻开始构建网格以及构建碰撞体,(玩家附近区块优先),
2?:
或者在工作线程?
网格构建和碰撞体生成依旧在下帧初始开始
//如果在工作线程,则网格构建系统必须每帧都固定调用多个工作,等待工作线程加入体素写入之后,并获取体素图里的dirty列表,构建网格,
//只是这样的话,这几个网格工作必须是顺序的,因为每个网格工作都必须在获取dirty区块后将其从dirty列表删除
//(想要同步,可以再多一个列表或集合,允许并行写入的那种,将被获取到的dirty区块写入列表,在每次获取的时候,检查是否被获取过)